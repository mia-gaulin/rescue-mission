<h1>Submit a new question</h1>

Questions should be at least 40 characters, and descriptions should be at least 150 characters.<br><br>

<%= form_for(@question) do |f| %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div><br />

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div><br />

  <div class="field">
    <%= f.submit "Submit question" %>
  </div>

<% end %>
